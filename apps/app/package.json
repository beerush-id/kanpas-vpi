{
  "name": "@beerush/kanpas-app",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite dev --port 3000 --open",
    "dev:core": "vite dev --port 3000 --open",
    "build": "vite build",
    "package": "vite package",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-check --tsconfig ./tsconfig.json --watch",
    "lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \"src\"",
    "format": "prettier --write --ignore-path=../../.prettierignore ."
  },
  "dependencies": {
    "@beerush/kanpas-component": "workspace:*",
    "@beerush/kanpas-core": "workspace:*",
    "@beerush/kanpas-engine": "workspace:*",
    "directory-tree": "^3.5.1"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "latest",
    "@sveltejs/kit": "latest",
    "svelte": "^3.54.0",
    "svelte-check": "^3.0.1",
    "svelte-preprocess": "^4.10.6",
    "tslib": "^2.3.1",
    "typescript": "^4.7.2",
    "vite": "^4.0.0"
  },
  "type": "module"
}
